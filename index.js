(function(){
  var express = require("express"),
    nano = require("nano"),
    socket = require("socket.io"),
    nodeStatic = require("express-static");

  var app = express();

  app.listen(8000);
});
